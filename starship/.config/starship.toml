palette = "melange"

# Insert a blank line between shell prompts
add_newline = true 

# Increase the default command timeout to 2 seconds
command_timeout = 2000

# Define the order and format of the information in our prompt
format = """\
[](fg:blue)\
$directory\
$conda\
[](fg:blue)\
$fill$cmd_duration\
$character"""

# Fill character (empty space) between the left and right prompt
[fill]
symbol = " "

[character]
format = '$symbol '
success_symbol = '[❯](white) '
error_symbol = '[❯](bold red)'
vimcmd_symbol = '[❮](bold green)'
vimcmd_visual_symbol = '[❮](bold yellow)'
vimcmd_replace_symbol = '[❮](bold magenta)'
vimcmd_replace_one_symbol = '[❮](bold magenta)'

# Disable the line break between the first and second prompt lines
[line_break]
disabled = true

# Customize the format of the working directory
[directory]
truncate_to_repo = false
format = "[ $path ]($style)"
style = "fg:foreground bg:#292522"

[git_branch]
symbol = " "
format = "[ $symbol$branch(:$remote_branch) ]($style)"
style = "fg:foreground bg:#292522"

[git_metrics]
disabled = false

[nodejs]
format = "via [$symbol($version )]($style)"
style = "yellow"

[package]
disabled = true # Enable to output the current working directory's package version
format = "[$version]($style) "
display_private = true

# Output the command duration if over 2 seconds
[cmd_duration]
min_time = 2_000
format = "[ $duration ]($style)"
style = "white"

[conda]
format = '[](fg:blue bg:#292522)[ $environment ](fg:green bg:#292522)'

# Customize the battery indicator
[battery]
format = "[$symbol $percentage]($style) "
empty_symbol = "🪫"
charging_symbol = "🔋"
full_symbol = '🔋'

[[battery.display]]
threshold = 10
style = 'red'

# Output a styled separator right after the directory when inside a git repository.
[custom.directory_separator_git]
description = "Output a styled separator right after the directory when inside a git repository."
command = ""
format = "[](fg:blue bg:#292522)"
# Only when inside git repository
when = "git rev-parse --is-inside-work-tree >/dev/null 2>&1"

# Output a styled separator right after the directory when NOT inside a git repository.
[custom.directory_separator_not_git]
description = "Output a styled separator right after the directory when NOT inside a git repository."
command = ""
format = "[](fg:blue)"
# Only when NOT inside a git repository
when = "! git rev-parse --is-inside-work-tree > /dev/null 2>&1"

[palettes.melange]
# Base colors
foreground = "#ECE1D7"
background = "#161312"

# UI colors
cursor_bg = "#ECE1D7"
cursor_border = "#ECE1D7"
cursor_fg = "#292522"
selection_bg = "#403A36"
selection_fg = "#ECE1D7"
scrollbar_thumb = "#7B9695"
split = "#7B9695"

# ANSI colors
black = "#34302C"
red = "#BD8183"
green = "#78997A"
yellow = "#E49B5D"
blue = "#7F91B2"
magenta = "#B380B0"
cyan = "#7B9695"
white = "#C1A78E"

# Bright ANSI colors
black_bright = "#867462"
red_bright = "#D47766"
green_bright = "#85B695"
yellow_bright = "#EBC06D"
blue_bright = "#A3A9CE"
magenta_bright = "#CF9BC2"
cyan_bright = "#89B3B6"
white_bright = "#ECE1D7"

# Additional melange colors (optional, for reference)
dark_black = "#292522"
dark_blue = "#273142"
dark_cyan = "#253333"
dark_green = "#233524"
dark_magenta = "#422741"
dark_red = "#7D2A2F"
dark_white = "#403A36"
dark_yellow = "#8B7449"
